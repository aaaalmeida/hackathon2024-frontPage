import { IMentor } from "./IMentor"

export const MentorCard = ({
    company,
    image,
    name,
    tags,
    text
}: IMentor) => {
    return (
        <article className="border border-cyan-500 rounded">

        </article>
    )
}